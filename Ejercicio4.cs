using System;


namespace CursoCSharp_Practicas
{
    class Program
    {
        static void Main(string[] args)

        {
            /* Guarde en la variable x (entera) el número 23, en la variable y (entera) el número 24,
             * en la variable z (entera) el número 25 y muestre en pantalla la suma de los valores de
             * las tres variables. */
            int x = 23, y = 24, z = 25;
            Console.WriteLine("El resultado de sumar {0} mas {1} mas {2} es igual a {3}", x, y, z, x + y + z);

        }
    }
}
